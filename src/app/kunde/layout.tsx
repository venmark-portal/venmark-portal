export const metadata = {
  title: 'Kundeoprettelse — Venmark Fisk A/S',
  robots: { index: false, follow: false },
}

export default function KundeLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
